= Dateibeschreibung

Die Datei `+SZ_INTERLIS_Konfiguration.xtf+` enthält die Daten nach dem Modell `+SZ_INTERLIS_Konfiguration+`. Änderungen an dieser Datei werden über GitHub versioniert und mit einer eindeutigen Identifikationsnummer (Commit-Hash-ID) versehen. Mithilfe dieser ID können frühere Datei-Versionen abgerufen werden.

Die folgende Tabelle fasst die relevanten Versionen der Datei zusammen:

[cols=*,options="header"]
|===
| Dateiname | Beschreibung | Modell (Version)
| https://raw.githubusercontent.com/ch-sz-geo/A256/main/data/SZ_INTERLIS_Konfiguration.xtf[*SZ_INTERLIS_Konfiguration.xtf*]
| Diese Datei enthält die aktuell gültigen Daten.
| SZ_INTERLIS_Konfiguration_V1 (2026-02-11)
|===
